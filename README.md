# Question No 1

## 1. Create a new folder for your project

* Created folder with name **GITQUESTIONNO1**

<img width="666" height="128" alt="image" src="https://github.com/user-attachments/assets/a893012a-0e7e-4f9e-ac7b-86abc830ed5f" />


## 2. Initialize a Git repository

* Command used:

  ```bash
  git init

<img width="824" height="234" alt="image" src="https://github.com/user-attachments/assets/61a3aedd-4017-4199-8a2b-9225dae1243c" />

* To initialise the repository

---

## 3. Create a file named `app.py` and add some Python code

* Added the `app.py` file with Python code

---

## 4. Check the current Git status

* Command used:

  ```bash
  git status
  ```

---

## 5. Stage the file

* Command used:

  ```bash
  git add .
  ```
* For staging files

---

## 6. Commit with a meaningful message

* Command used:

  ```bash
  git commit -m "addition code"
  ```

---

## 7. Create a remote repository (GitHub or similar)

* Repository created

---

## 8. Add the remote (origin) to your local repo

* Command used:

  ```bash
  git remote add origin https://github.com/patilprerana27-prog/GitQuestionNo1.git
  ```

---

## 9. Verify the remote configuration

* Command used:

  ```bash
  git remote -v
  ```

---

## 10. Push your code to the remote repository

* Commands used:

  ```bash
  git branch -M main
  git push -u origin main
  ```
